# Ensure all text files use LF (Unix-style)
* text=auto eol=lf

# Shell scripts always use LF
*.sh text eol=lf

# Python files always use LF
*.py text eol=lf

# Windows batch files always use CRLF
*.bat text eol=crlf

# Treat images and binaries correctly
*.png binary
*.jpg binary
*.zip binary
*.pdf binary
